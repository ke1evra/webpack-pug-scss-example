include view/variables.pug

doctype html
html(lang="ja")
  head
    meta(charset="UTF-8")
    meta(name="description" content=pkgData.description)
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    meta(http-equiv="X-UA-Compatible", content="ie=edge")
    link(
      rel="stylesheet"
      type="text/css"
      href="https://cdn.jsdelivr.net/npm/yakuhanjp@3.2.0/dist/css/yakuhanjp-noto.min.css"
    )
    title #{pkgData.title} | #{pkgData.name}
  body.body
    include view/header.pug
    main.main
      .mps-hero
        //- img(src="" alt="foo")
      P a main block
    include view/footer.pug
